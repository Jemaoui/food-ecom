<!DOCTYPE html>
<html>
<head>
    <title>Invoice - Order #{{ order.id }}</title>
    <style>
        body { font-family: Arial, sans-serif; }
        .invoice { margin: 20px; }
        table { width: 100%; border-collapse: collapse; }
        th, td { border: 1px solid #ddd; padding: 8px; }
        th { background-color: #f2f2f2; }
    </style>
</head>
<body>
    <div class="invoice">
 

    <h1>WELLFOOD</h1>
        <h2>Invoice - Order #{{ order.id }}</h2>
        <p><strong>Date:</strong> {{ order.createdAt ? order.createdAt|date('Y-m-d H:i:s') : '' }}</p>
        <p><strong>Customer:</strong> {{ order.customer.username }}</p>

        <h3>Order Items</h3>
        <table>
            <thead>
                <tr>
                    <th>Product</th>
                    <th>Quantity</th>
                    <th>Unit Price</th>
                    <th>Total</th>
                </tr>
            </thead>
            <tbody>
                {% for item in order.orderItems %}
                    <tr>
                        <td>{{ item.product.name }}</td>
                        <td>{{ item.quantity }}</td>
                        <td>{{ item.price|number_format(2, '.', ',') }} USD</td>
                        <td>{{ (item.quantity * item.price)|number_format(2, '.', ',') }} USD</td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>

        <h3>Total: {{ order.totalAmount|number_format(2, '.', ',') }} USD</h3>
    </div>
</body>

        <div class="footer">
            <p>Thank you for your business!</p>
            <p>Company Name | Address | Phone</p>
        </div>
</html>
